# Local WMF converter

This directory contains the Ubuntu `libwmf-bin` runtime extracted without
installing system packages. `scripts/build_exam_diagnosis_bank.py` uses
`usr/bin/wmf2svg` to convert legacy Word equation images into browser-readable
SVG assets. The package version and SHA-256 are recorded in the generator.
